"""
Data Splitter

This script is responsible for partitioning the raw Wisconsin breast cancer dataset
into a training set and a validation set. It strictly uses pandas for ingestion
and manipulation.
"""
import pandas as pd
import numpy as np

def split_data(csv_path: str, train_ratio: float = 0.8):
    """
    Reads the dataset and splits it into training and validation sets.
    """
    pass

if __name__ == "__main__":
    print("Running split phase...")
    # split_data('data/data.csv')
